import React from "react";
import "./Button.scss";

const Button = () => {
  return (
    <div className="wrapper">
      <button className="HelloBtn">Hello,World!! Me</button>
    </div>
  );
};

export default Button;
